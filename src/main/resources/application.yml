spring:
  application:
    name: approval-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://mongo:McrJAYsOqxvkuEdxmsIllStmCkmoFunZ@mongodb.railway.internal:27017/approval_service}
  management:
    endpoints:
      web:
        exposure:
          include: health,info,metrics
    endpoint:
      health:
        show-details: always
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: ${PORT:8081}

logging:
  level:
    com.bom.approval: DEBUG
